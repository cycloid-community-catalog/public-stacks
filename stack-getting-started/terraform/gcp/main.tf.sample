module "instance" {
  source       = "./module-gcp"
  env          = var.env
  organization = var.organization
  project      = var.project

  // Gcp
  gcp_zone      = var.gcp_zone
  gcp_project   = var.gcp_project
}
